language: go
os: linux
dist: focal
sudo: required

go:
  - "1.19.x"

before_install:
  - sudo -E bash ./scripts/travis-linux.sh

install:
  - make lint-deps

script:
  - sudo -E "PATH=$PATH" make check test
